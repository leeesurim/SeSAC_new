<html>
    <head>
        <title>EJS</title>
    </head>
    <body>
        <!-- <p>
            <%=a%>
        </p> -->

        <% for (var i = 0; i < list.length; i++) { %>
            <!-- 백 내용을 프론트로 보여주는 방법 -->
            <h1><%=list[i]%></h1>
        <% } %>

        <!-- <% %> 한줄씩 작성해야 함  -->
        <!-- for ( var i = 0; i < 5; i++){
            if ( i == 3 ){
                // h3 태그
            } else {
                // h1 태그
            }
        } -->
        <!-- <% for (var i = 0; i < 5; i++) { %>
            <% if (i == 3) { %>
                <h3>hi</h3>
            <% } else { %>
                <h1>hi</h1>
            <% } %>
        <% } %> -->

        <!-- <%=a%> a 변수의 값을 보여주기 -->
        <% var a = 'hello'; %>
        <p><%=a%></p>

        <!-- 파일 안에서 다른 파일 불러와서 보여줄 수 있음-->
        <%- include('test2.ejs') %>
        
    </body>
</html>